// Напишите функцию myAdd(), которая должна принимать три параметра, складывать их и возвращать полученное значение. Вызывать функцию не надо

<?php
function myAdd($a, $b, $c) {
  $f = $a + $b + $c;
  return $f;
}
?>
